# Vanompp v1.2 — UI Update + Icon Vano

**Date:** 2026-08-05
**Status:** Draft → pending approval
**Depends on:** v1 (2026-08-04), v1.1 (2026-08-05) — bugfix Not Responded + Port Persist
**Out of scope:** landing page `vanompp.my.id`, OG image, brand kit, audit bugfix

## Problem

v1.1 functional (port persist + not responded fix) tapi branding masih huruf V generic, README kosong/over-teknikal. v1.2 perlu public-ready look: mascot Vano jadi icon + UI polish minimal.

## Scope v1.2 Lock

- UI update + ganti icon pakai mascot Vano (gajah pink `icon baru.png`)
- README hybrid SMK-first, quickstart tetap ada, tree installer after install ada, refer docs spec cukup sebut file (no deep dive teknis)
- No logic change, no new dep, no refactor audit findings

## Pre-work (sudah di disk, validasi saja)

Source: `icon baru.png` 1506x1044 RGBA → PIL square crop center -60px upper →

- `public/mascot.png` 512x512 158K — buat web + App header
- `src-tauri/icons/icon.png` 1024 482K
- `src-tauri/icons/128x128.png`, `128x128@2x.png`, `32x32.png`
- `src-tauri/icons/icon.ico` multi 16-256
- `Square30x30Logo…310x310Logo + StoreLogo.png`
- `docs/images/mascot-original.png` + `mascot-512.png`
- `src/App.svelte` header V → `<img /mascot.png w-10 h-10 rounded-12 shadow>` done
- `index.html` favicon vite.svg → mascot.png + title vanompp → Vanompp portable tagline done
- `README.md` 9.1K draft existing — keteknikan (audit, stack detail, services.rs internals) → need rewrite hybrid

Unstaged: 14 icons modified di git status (belum commit).

## Design

### 1. Icon final

- Verify all icons dari crop yang sama, no quality loss
- `tauri.conf.json` sudah refs `icon.png`/`ico` — no change needed
- Source `icon baru.png` root — after commit pindah to `docs/images/` atau keep? Decision: keep original in `docs/images/mascot-original.png` (sudah ada), `icon baru.png` boleh dihapus / masuk gitignore (large 640K binary in root)
- Favicon `public/mascot.png` already wired via `index.html`

### 2. UI Update

**Header:** keep Vano 40px rounded-12 shadow, dark contrast ok di `#0a0a0a` bg (#0a0a0a vs white bg image)

**EmptyState:** `src/lib/components/EmptyState.svelte` sekarang:
- Box dashed www/ → localhost:8080/{folder} + text kosong + CTA
Ganti: tambah Vano small 64px rounded-2xl di atas, copy tweak "Belum ada project — Vano siap nemenin" + existing flow tetap, CTA focus `#create-input` existing `handleEmptyCta` stay

**Other components:** no redesign — ServiceCard, ProjectCard, CreateCard, PortConflictModal, LogViewer, Toast, BezelCard keep volt tokens

### 3. README hybrid

Top: `<p align=center><img public/mascot.png 160><br><b>Vano</b> gajah Apache santai>`

Sections:
- Kenapa Vanompp? 3 bullet Laragon kebanyakan tombol, 90% stuck path, USP anti-bingung > ringan bonus (Santai Indo)
- Quickstart: `npm i`, `npm run tauri:dev`, `npm run build + tauri` + download bin note `pwsh scripts/download-bin.ps1`
- Cara Pakai 6 langkah SMK: Start MySQL→Apache→Buat Project→Salin URL gede→conn.php→phpMyAdmin→Tetap pakai flow
- Tree after install:
  - Portable: folder extract `Vanompp.exe`, `www/`, `bin/apache|php|mysql`, `vanompp.toml`
  - Installer: `C:\Vanompp\` same layout, shortcuts Desktop+StartMenu
- License non-komersial refer `LICENSE` + link GitHub
- Docs specs: list files `docs/superpowers/specs/2026-08-04-v1`, `2026-08-05-v1.1`, `2026-08-05-v2-installer` — no deep dive

Hapus dari README lama: stack detail panjang, audit bug list, services.rs internals, config.rs atomic write details, slug hard list — pindah ke specs / keep lean

### 4. Out-of-scope

- No landing page, no OG 1200x630, no brand kit board, no audit bugfix (greet dead, create_project_with_port unused, create_db alias, get import dead, toast double, LogViewer prop mutate, multi-conflict single button), no new deps

## Error Handling

No logic change → error paths unchanged from v1.1.

## Testing / Done criteria

- `npm run check` 0 errors
- `npm run build` pass
- `src-tauri/icons/icon.png` + `icon.ico` exist, header `<img /mascot.png>` visible
- EmptyState shows Vano img + CTA focus works
- README top shows mascot, Quickstart copy-paste runnable, tree installer vs portable documented

## File Changes

- MOD `src/lib/components/EmptyState.svelte` — add Vano img
- MOD `README.md` — rewrite hybrid
- MOD `src-tauri/icons/*` — already generated, just verify+commit
- OPTIONAL del `icon baru.png` root or gitignore + keep reference in `docs/images/`
- NO change `src/App.svelte` header already Vano, `index.html` favicon already, `tauri.conf.json` icons already

## Migration

No DB migration. Users existing v0.1.0/v1.1 get new icon after rebuild exe.

## Self-review

- Placeholder scan: none TBD — all file paths exist, `public/mascot.png` verified 158K
- Internal consistency: pre-work listed matches git status 14 modified icons + README + App + index.html; scope "UI+icon only" matches file changes (EmptyState+README+icons); no contradict audit fix vs out-of-scope
- Scope check: focused single purpose (branding UI+icon), no subsystem split needed, 2 files edit + icons commit = single plan feasible
- Ambiguity: "Vano everywhere" clarified as header+icon pack+EmptyState small, not full illustration rework; README "jangan teknikal" clarified as hapus stack/audit details keep quickstart+tree+license
